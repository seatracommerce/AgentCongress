-r requirements.txt
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # async test client for FastAPI
pytest-mock>=3.14.0    # mocker fixture
aiosqlite>=0.20.0      # in-memory SQLite for DB tests (no Postgres needed)
